# Changelog

## 2026-03-10
- 新增顶层 `README.md`，明确 `codex一键安装技能/` 面向 `Codex CLI`，`cursor一键安装技能/` 面向 `Cursor`。
- 新增顶层兼容矩阵，统一说明两套包的技能目录、MCP 配置文件、Context7 启动器和安装后重启目标。
- 修复 `Cursor` 包内残留的 `Codex skill collection` 文案。
- 统一两套包的安装/修复脚本输出，显式标注当前正在执行的是 `Codex package` 还是 `Cursor package`。
