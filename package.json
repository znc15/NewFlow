{
  "name": "newflow",
  "version": "0.4.2",
  "license": "MIT",
  "bin": {
    "flow": "./dist/flow.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/main.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "test:smoke": "vitest run src/application/workflow-service.test.ts src/application/workflow-service.verify.test.ts src/infrastructure/fs-repository.test.ts src/infrastructure/git.test.ts src/infrastructure/runtime-state.test.ts src/infrastructure/verify.test.ts src/e2e/operational-readiness.test.ts",
    "setup": "npm run build && npm link"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
